## Customer Service Foundation

1. Create the `enums` package with `Gender`, `AddressType`, and `CustomerStatus`.
2. Add `Customer` and `CustomerAddress` entities in the root customer-service module.
3. Keep the JPA mappings and timestamp behavior aligned with the service standard.
4. Add repository interfaces with the expected lookup methods.
5. Add request and response DTOs for customers and addresses.
6. Add the customer mapper for entity/DTO conversions.
7. Verify the module compiles cleanly after the model changes.
